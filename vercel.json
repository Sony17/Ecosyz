{
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "framework": "nextjs",
  "buildCommand": "npm run vercel-build",
  "installCommand": "npm ci",
  "env": {
    "PRISMA_GENERATE": "true",
    "NODE_ENV": "production",
    "PRISMA_MIGRATE_ALLOW_RESET": "false"
  },
  "version": 2,
  "regions": ["iad1"],
  "build": {
    "env": {
      "NODE_VERSION": "20.x"
    }
  }
}
